---
- homebrew:
    update_homebrew: yes
    upgrade_all: yes

- homebrew_tap:
    name:
      - cloudfoundry/tap

- homebrew:
    name:
      - bash
      - curl
      - ghq
      - git
      - jq
      - peco
      - tree
      - vim
      - wget
      # kubernetes
      - kubectx
      - kubernetes-cli
      - kube-ps1
      - stern
      # cloud foundry
      - cf-cli
