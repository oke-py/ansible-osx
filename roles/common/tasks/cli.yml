---
- homebrew:
    update_homebrew: yes
    upgrade_all: yes

- name: Tap a Homebrew repository, state present
  community.general.homebrew_tap:
    name: hashicorp/tap

- homebrew:
    name:
      - asdf
      - bash
      - bat
      - curl
      - fzf
      - ghq
      - git
      - hashicorp/tap/terraform
      - jq
      - tree
      - wget
      - yq
