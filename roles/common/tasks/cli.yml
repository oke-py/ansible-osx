---
- homebrew:
    update_homebrew: yes
    upgrade_all: yes

- homebrew_tap:
    name: superbrothers/zsh-kubectl-prompt

- homebrew:
    name:
      - asdf
      - bash
      - curl
      - ghq
      - git
      - jq
      - peco
      - tree
      - wget
      # kubernetes
      # - kubernetes-cli
      # - kubectx
      - stern
      - zsh-kubectl-prompt
