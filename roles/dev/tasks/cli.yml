---
- homebrew_tap:
    name:
      - argoproj/tap
      - aws/tap
      - heroku/brew
      - snyk/tap

- homebrew:
    name:
      - coreutils
      - clang-format
      - gnu-sed
      - gnu-tar
      - protobuf
      - terraform
      # lang
      - dep
      - go
      - python
      - ruby
      # aws
      - aws-iam-authenticator
      # kubernetes
      - argocd
      - istioctl
      - operator-sdk
      - skaffold
      # heroku
      - heroku
      - heroku-node
      # security
      - cfssl
      - nmap
      - snyk
      - telnet

